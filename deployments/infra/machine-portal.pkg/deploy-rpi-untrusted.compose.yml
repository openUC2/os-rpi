services:
  server-untrusted:
    volumes:
      - type: bind
        source: /run/machine-name
        target: /run/machine-name
        read_only: true
      # - type: bind
      #   source: /etc/machine-portal/web/templates
      #   target: /web/templates
      #   read_only: true
