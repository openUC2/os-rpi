services:
  server-untrusted:
    environment:
      - MACHINENAME_NAME=dev-machine
    # volumes:
    #   - type: bind
    #     source: $FORKLIFT_STAGED/etc/machine-portal/web/templates
    #     target: /web/templates
    #     read_only: true
