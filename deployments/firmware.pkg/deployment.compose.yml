services:
  server:
    image: ghcr.io/youseetoo/firmware-image-server:sha-b7cf4af
    volumes:
      - server-data:/data
      - server-config:/config
volumes:
  server-data: null
  server-config: null
