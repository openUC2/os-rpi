services:
  server:
    image: ghcr.io/youseetoo/firmware-image-server:sha-285449d
    volumes:
      - server-data:/data
      - server-config:/config
volumes:
  server-data: null
  server-config: null
