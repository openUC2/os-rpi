services:
  server:
    image: ghcr.io/youseetoo/firmware-image-server:sha-ca46b0c
    volumes:
      - server-data:/data
      - server-config:/config
volumes:
  server-data: null
  server-config: null
