services:
  server:
    image: ghcr.io/youseetoo/firmware-image-server:sha-95902fc
    volumes:
      - server-data:/data
      - server-config:/config
volumes:
  server-data: null
  server-config: null
