services:
  reverse-proxy-config:
    networks:
      - caddy-ingress
    labels:
      caddy: http://, https://, openuc2-sweet-lead-1137.queue-macaroni.ts.net
      caddy.redir: /admin/cockpit /admin/cockpit/
      caddy.handle: /admin/cockpit/*
      caddy.handle.reverse_proxy: host.docker.internal:9090

networks:
  caddy-ingress:
    external: true
