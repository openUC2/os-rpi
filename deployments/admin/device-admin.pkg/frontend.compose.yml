services:
  server:
    networks:
      - caddy-ingress
    labels:
      caddy: http://, https://, *.queue-macaroni.ts.net
      caddy.redir: /admin/panel /admin/panel/
      caddy.handle: /admin/panel/*
      caddy.handle.reverse_proxy: "{{upstreams 3001}}"

networks:
  caddy-ingress:
    external: true
